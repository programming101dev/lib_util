# lib_util (retired)

This repository is no longer part of the p101 workspace.

- Byte-order conversion moved to `lib_endian`.
- Subprocess execution moved to `lib_subprocess`.

The repository remains only as migration history. New code must depend on the
narrow replacement library that owns the required API.
